import React, { FunctionComponent } from 'react';

const PlaygroundFooter: FunctionComponent = () => {
    return (
        <div>
            <button type="button">-1</button>
            <button type="button">0</button>
            <button type="button">+1</button>
        </div>
    );
};

export default PlaygroundFooter;
